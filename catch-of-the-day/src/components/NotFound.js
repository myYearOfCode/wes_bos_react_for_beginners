import React from "react";

//this is the way to convert it to a functional stateless component.
const Header = (props) => {
    return (
      <div className="notFound">NOT FOUND!</div>
    )
  }

export default Header;
